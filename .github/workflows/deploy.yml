name: Deploy

on:
  push:
    branches: [main]

jobs:
  ci:
    name: Lint, Build, Test
    uses: ./.github/workflows/ci.yml

  deploy:
    name: Deploy
    needs: [ci]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            cd ~/kanban-task
            git pull origin main
            git submodule update --init
            docker build -t kanban-task .
            docker stop kanban-task-app || true
            docker rm kanban-task-app || true
            docker run -d \
              --name kanban-task-app \
              --network kanban-network \
              --env-file .env \
              -p 3002:3002 \
              --restart always \
              kanban-task
            docker image prune -f
